# Bundle Tests

bundle.test.mjs uses only Node built-ins. It checks the standalone app boundary, Lenis/GSAP/static-export contract, all five safe SVG assets and the exact dimensions of all three PNG assets.
